<UserControl x:Class="ImporterPOS.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ImporterPOS.WPF.Views"
             xmlns:modalcontrol="clr-namespace:ModalControl;assembly=ModalControl"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="10 70">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Background="#1073B1" CornerRadius="10">
            <TabControl Grid.Row="0" Grid.ColumnSpan="2">
                <TabControl.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1, 0.7">
                        <GradientStop Color="{StaticResource secondaryBackColor2}" Offset="0"/>
                        <GradientStop Color="{StaticResource secondaryBackColor1}" Offset="1"/>
                        <GradientStop Color="{StaticResource InformationColor}" Offset="0.3"/>
                    </LinearGradientBrush>
                </TabControl.Background>
                <TabItem Header="Postavke">
                    <Grid Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                                <Label Grid.Row="0" Content="Kreiraj robu na osnovu artikla" Style="{StaticResource settingsControlLabel}" />
                                <Button Grid.Column="1"  Content="Odaberi" Height="35" Width="80" Margin="10 0" Style="{StaticResource settingsClickButton}" Command="{Binding CreateGoodsBasedOnArticleNameCommand}" />
                                <Label Grid.Row="1" Content="Nuliraj skladište" Style="{StaticResource settingsControlLabel}" />
                                <Button Grid.Row="1" Grid.Column="1" Content="Odaberi" Height="35" Width="80" Margin="10 0" Style="{StaticResource settingsClickButton}" Command="{Binding SetMainStockToZeroCommand}" />
                        <Label Grid.Row="2" Content="Korekcija skladišta (Excel)" Style="{StaticResource settingsControlLabel}" />
                        <Button Grid.Row="2" Grid.Column="1" Style="{StaticResource settingsClickButton}" Content="Odaberi" Command="{Binding StockCorrectionFromExcelCommand}" Height="35" Width="80"/>
                        <Label Grid.Row="3" Content="Korekcija skladišta (BarCode čitač)" Style="{StaticResource settingsControlLabel}" />
                        <Button Grid.Row="3" Grid.Column="1" Style="{StaticResource settingsClickButton}" Content="Odaberi" Command="{Binding ReadBarcodeTxtFileCommand}" Height="35" Width="80"/>

                    </Grid>
                </TabItem>
                <TabItem Header="Postavke baze podataka">
                    <Grid Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                            <Label Content="Instanca servera:" Style="{StaticResource settingsControlLabel}" />
                            <TextBox Grid.Column="1" IsReadOnly="True" Width="300" Height="30" FontSize="18" Text="{Binding ServerInstance, Mode=TwoWay}" />
                            <Label Grid.Row="1" Content="Baza podataka:" Style="{StaticResource settingsControlLabel}" />
                            <TextBox Grid.Row="1" Grid.Column="1" IsReadOnly="True" Width="300" Height="30" FontSize="18" Text="{Binding DatabaseConnection, Mode=TwoWay}" />
                            <Label Grid.Row="2" Content="Port:" Style="{StaticResource settingsControlLabel}" />
                            <TextBox Grid.Row="2" Grid.Column="1" IsReadOnly="True" Width="300" Height="30" FontSize="18" Text="{Binding Port, Mode=TwoWay}" />
                    </Grid>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>
