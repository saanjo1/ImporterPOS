<UserControl x:Class="ImporterPOS.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ImporterPOS.WPF.Views"
             xmlns:modalcontrol="clr-namespace:ModalControl;assembly=ModalControl"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="10 70">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border CornerRadius="10" Background="#1073B1">
            <TabControl Grid.Row="0" Grid.ColumnSpan="2">
                <TabControl.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1, 0.7">
                        <GradientStop Color="{StaticResource secondaryBackColor2}" Offset="0"/>
                        <GradientStop Color="{StaticResource secondaryBackColor1}" Offset="1"/>
                        <GradientStop Color="{StaticResource InformationColor}" Offset="0.3"/>
                    </LinearGradientBrush>
                </TabControl.Background>
                <TabItem Header="Postavke">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="1">
                            <StackPanel.Resources>
                                <Style TargetType="Button" BasedOn="{StaticResource settingsClickButton}">
                                    <Setter Property="Margin" Value="10 0" />
                                    <Setter Property="Width" Value="80" />
                                    <Setter Property="Height" Value="35" />
                                </Style>
                            </StackPanel.Resources>
                            <StackPanel Orientation="Horizontal">
                                <Label Content="Kreiraj robu na osnovu artikla" Style="{StaticResource settingsControlLabel}" />
                                <Button Content="Odaberi" Command="{Binding CreateGoodsBasedOnArticleNameCommand}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Label Content="Nuliraj skladište" Style="{StaticResource settingsControlLabel}" />
                                <Button Content="Odaberi" Command="{Binding SetMainStockToZeroCommand}" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </TabItem>
                <TabItem Header="Postavke baze podataka">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                            <Label Content="Instanca servera:" Style="{StaticResource settingsControlLabel}" />
                            <TextBox IsReadOnly="True" Width="300" Height="30" FontSize="18" Text="{Binding ServerInstance, Mode=TwoWay}" />
                        </StackPanel>
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <Label Content="Baza podataka:" Style="{StaticResource settingsControlLabel}" />
                            <TextBox IsReadOnly="True" Width="300" Height="30" FontSize="18" Text="{Binding DatabaseConnection, Mode=TwoWay}" />
                        </StackPanel>
                        <StackPanel Grid.Row="3" Orientation="Horizontal">
                            <Label Content="Port:" Style="{StaticResource settingsControlLabel}" />
                            <TextBox IsReadOnly="True" Width="300" Height="30" FontSize="18" Text="{Binding Port, Mode=TwoWay}" />
                        </StackPanel>
                    </Grid>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>
