<UserControl x:Class="ImporterPOS.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImporterPOS.WPF.Views"
             xmlns:modalcontrol="clr-namespace:ModalControl;assembly=ModalControl" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="10 70">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <Border Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label HorizontalAlignment="Left" FontSize="18" FontWeight="SemiBold" Foreground="White" Content="Excel postavke:" Margin="0 0 0 20"/>


                <Label HorizontalAlignment="Left" Grid.Row="1" Content="Odaberi Excel file" Style="{StaticResource settingsControlLabel}"></Label>
                <Button Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" Margin="10 0" Style="{StaticResource settingsClickButton}" Content="Odaberi" Command="{Binding UploadExcelFileCommand}" Width="80" Height="35"/>
                <fa:IconImage HorizontalAlignment="Left" Grid.Row="1" Grid.Column="2" Icon="Check" Foreground="LightGreen" Height="20" Width="20" Visibility="{Binding SelectFileSuccess, Converter={StaticResource BoolToVis}}"/>

                <Label HorizontalAlignment="Left" Grid.Row="2" Content="Odaberi sheet sa filea" Style="{StaticResource settingsControlLabel}"></Label>
                <Button Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right" Margin="10 0" Style="{StaticResource settingsClickButton}" x:Name="btnSheet" Content="Odaberi" Command="{Binding SelectSheetCommand}" Width="80" Height="35"/>
                <fa:IconImage HorizontalAlignment="Left" Grid.Row="2" Grid.Column="3" Icon="Check" Foreground="LightGreen" Height="20" Width="20" Visibility="{Binding SelectSheetSuccess, Converter={StaticResource BoolToVis}}"/>

            </Grid>
        </Border>

        <Border Grid.Column="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Label HorizontalAlignment="Left" FontSize="18" FontWeight="SemiBold" Foreground="White" Content="Postavke baze podataka:" Margin="0 0 0 30"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Instanca servera:" Style="{StaticResource settingsControlLabel}"></Label>
                <TextBox Grid.Row="1" Grid.Column="1" IsReadOnly="True" Width="300" Height="30" FontSize="18" Text="{Binding ServerInstance, Mode=TwoWay}"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Baza podataka:" Style="{StaticResource settingsControlLabel}"></Label>
                <TextBox Grid.Row="2" Grid.Column="1" IsReadOnly="True" Width="300" Height="30" FontSize="18" Text="{Binding DatabaseConnection, Mode=TwoWay}"/>

                <Label Grid.Column="0" Grid.Row="3" Content="Port:" Style="{StaticResource settingsControlLabel}"></Label>
                <TextBox Grid.Column="1" Grid.Row="3" IsReadOnly="True" Width="300" Height="30" FontSize="18" Text="{Binding Port, Mode=TwoWay}"/>

            </Grid>
        </Border>

        <Border Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label HorizontalAlignment="Left" FontSize="18" FontWeight="SemiBold" Foreground="White" Content="Napredne postavke:" Margin="0 0 0 20"/>


                <Label HorizontalAlignment="Left" Grid.Row="1" Content="Kreiraj robu za artikle" Style="{StaticResource settingsControlLabel}"></Label>
                <Button Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" Margin="10 0" Style="{StaticResource settingsClickButton}" Content="Spoji" Command="{Binding CreateGoodsBasedOnArticleNameCommand}" Width="80" Height="35"/>
                <fa:IconImage HorizontalAlignment="Left" Grid.Row="1" Grid.Column="2" Icon="Check" Foreground="LightGreen" Height="20" Width="20" Visibility="{Binding SelectFileSuccess, Converter={StaticResource BoolToVis}}"/>

                <Label HorizontalAlignment="Left" Grid.Row="2" Content="Nuliraj skladište" Style="{StaticResource settingsControlLabel}"></Label>
                <Button Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right" Margin="10 0" Style="{StaticResource settingsClickButton}" Content="Odaberi" Command="{Binding SetMainStockToZeroCommand}" Width="80" Height="35"/>

            </Grid>
        </Border>

    </Grid>

</UserControl>
